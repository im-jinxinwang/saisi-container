# saisi-container

Docker image build scripts for Tomcat with Dragonwell JDK.

## Build Scripts

- `build-bellsoft.sh` - Build with BellSoft JDK
- `build-Dragonwell.sh` - Build with Alibaba Dragonwell JDK

## Images

- `images/tomcat/` - Tomcat Docker image configuration
